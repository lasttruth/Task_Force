<%= form_for ([@game, @gane.status.build]) do |f| %>


  <%= f.label :condition %>:
  <%= f.text_field :condition, {:class => 'form-control'} %><br />

  <%= f.label :completion %>:
  <%= f.number_field :completion, {:class => 'form-control'} %><br />

  <%= f.submit %>

<%end%>
